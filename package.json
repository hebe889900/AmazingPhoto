{
  "name": "amazing-photo-app",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix pnf-server && npm install --prefix pnf-client",
    "build": "npm run-script build --prefix pnf-client",
    "start": "npm run-script build --prefix pnf-client & npm start --prefix pnf-server"
  },
  "dependencies": {
    "redux": "^3.7.2"
  },
  "devDependencies": {
    "bootstrap": "^4.0.0",
    "jquery": "^3.3.1",
    "popper.js": "^1.14.3"
  }
}
